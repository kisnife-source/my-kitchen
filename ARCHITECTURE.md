# 我的小厨房：运行时结构

当前稳定界面基线：**V0.5.5**。本次整理不改变 V0.5.4 的主要界面和交互，只收敛代码加载结构。

## 浏览器实际加载

```
index.html
├─ assets/app.css
├─ src/runtime-core.js
├─ hoc-recipes.generated.js
└─ src/runtime-features.js
```

### `index.html`
只负责页面壳、底部导航、版本号和运行时加载顺序。

### `assets/app.css`
当前唯一运行时样式入口。它按原 V0.5.4 的 CSS 加载顺序合并，确保整理前后的视觉结果尽量一致。

### `src/runtime-core.js`
基础数据结构、localStorage 状态、通用工具函数和早期稳定逻辑。

### `hoc-recipes.generated.js`
自动生成的外部菜谱数据。它保持独立，因为数据体积大且有独立同步流程。

### `src/runtime-features.js`
库存、按食材找菜、今日菜单、自定义菜谱、首页、设置、主题和 V0.5.4 UI 修正等功能的当前运行时实现。

## 旧 patch 文件

根目录中的 `patch-*.js/css` 是历史迭代记录，**index.html 已不再引用它们**。Git 历史本身也可追溯这些版本，因此后续确认 V0.5.5 稳定后可以统一删除或归档，不应再新增新的 patch 文件。

## 后续开发约定

1. 不再通过新增 `patch-vXXXX.*` 叠加功能。
2. 小改动直接进入对应运行时文件；较大功能再按模块拆分。
3. 用户可见版本号只由 `window.MK_VERSION` 管理。
4. 菜谱数据与 UI 代码继续分离，生成数据保持独立。
5. 每次结构调整优先保证以下主流程不回归：
   - 首页 → 冰箱
   - 菜谱搜索 / 筛选 / 按食材找
   - 菜谱 → 今日菜单
   - 今日菜单 → 集中准备 → 灶台
   - 购物清单 → 买到 → 写回库存
   - 自定义菜谱创建 / 编辑 / 删除
